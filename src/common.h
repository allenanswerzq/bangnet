#ifndef BANGNET_COMMON_H_
#define BANGNET_COMMON_H_

#include <glog/logging.h>

#include <map>
#include <set>
#include <cmath>
#include <vector>
#include <string>
#include <climits>
#include <sstream>
#include <utility>
#include <fstream>
#include <iostream>

namespace bangnet {
  using std::fstream;
  using std::ios;
  using std::isnan;
  using std::isinf;
  using std::iterator;
  using std::make_pair;
  using std::map;
  using std::ostringstream;
  using std::pair;
  using std::set;
  using std::string;
  using std::stringstream;
  using std::vector;
}  // namespace bangnet

#endif  // BANGNET_COMMON_H_